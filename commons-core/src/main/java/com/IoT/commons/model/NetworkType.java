package com.IoT.commons.model;

public enum NetworkType {
    TRAFFIC,        // road sensors    — speed km/h
    ENVIRONMENT,    // weather stations — temp °C
    ENERGY,         // smart meters    — kWh
    INDUSTRIAL      // factory machines — vibration mm/s
}
